# Godot 4 生成物（絶対にコミットしない）
.godot/
*.import
.import/
*.tmp
*.translation

# エクスポート（プリセットは共有する。署名などの秘密は Godot 4 が export_credentials.cfg に分けるのでそちらだけ除外）
export_credentials.cfg
export/
build/
*.pck

# エディタ・OS
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Python（docs/tools のビルドスクリプト用）
__pycache__/
*.pyc

# field_scaffold の出力（events.json / messages.json へ貼り込んだら削除する）
data/skeletons/
